# Server Configuration
PORT=3000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=demo_app
DB_USER=postgres
DB_PASSWORD=your_password_here

# Connection Pool Settings (optional, defaults shown)
# DB_POOL_MIN=2
# DB_POOL_MAX=10

# Shutdown Configuration (optional, defaults shown)
# SHUTDOWN_TIMEOUT=10000
